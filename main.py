"""Точка входа в приложение ShelterShare.

Этот модуль запускает серверную часть (Flask API), к которой позднее
подключится десктопный клиент PySide6. Для разработки используется
переменная окружения FLASK_ENV и файл .env с секретами.
"""
from backend.app import create_app

app = create_app()


if __name__ == "__main__":
    # Комментарий на русском: включаем режим разработки при прямом запуске
    app.run(debug=True)
