{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export PYTHONPATH=$PYTHONPATH:. && python scripts/init_db.py && python scripts/seed_basics.py && gunicorn --bind 0.0.0.0:$PORT main:app",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}